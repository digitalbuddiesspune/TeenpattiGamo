# Local admin → Teen Patti admin API (server on :4200).
# Leave blank / relative paths so Vite proxies to localhost:4200.
VITE_API_BASE_URL=/api/v1/admin/profit-loss
VITE_AUTH_API_BASE_URL=/api/v1/admin/auth
